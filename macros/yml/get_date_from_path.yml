version: 2

macros:
  - name: get_date_from_path
    description: Macro used to retrieve the date from the file path.
    docs:
      show: true
    arguments:
      - name: column_name
        type: string
        description: Column name or expression that contains the path.